# Email delivery uses FormSubmit (https://formsubmit.co) — no API keys.
# The recipient email is read from SITE.contact.primaryEmail in lib/site.ts.

# Production base URL — also used for canonical / sitemap / OG / schema.
SITE_URL=http://localhost:3000
